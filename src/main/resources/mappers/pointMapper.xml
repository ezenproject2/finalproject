<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ezen.books.repository.PointMapper">

    <insert id="insertPoint" parameterType="com.ezen.books.domain.PointsVO">
        insert into points (mno, orno, earned, used, balance, reg_at)
        values (#{mno}, #{orno}, #{earned}, #{used}, #{balance}, #{regAt})
    </insert>

    <update id="updateBalance" parameterType="com.ezen.books.domain.PointsVO">
        update points
        set balance = #{balance}
        where mno = #{mno} and orno = #{orno}
    </update>

    <select id="getPointByMember" resultType="com.ezen.books.domain.PointsVO">
        select * from points
        where mno = #{mno}
        order by reg_at desc
    </select>

    <select id="getLastPointByMember" resultType="com.ezen.books.domain.PointsVO">
        select * from points
        where mno = #{mno}
        order by reg_at desc limit 1
    </select>

    <select id="getPointByMember" resultType="com.ezen.books.domain.PointsVO">
        select * from points
        where mno = #{mno}
        order by reg_at
    </select>

</mapper>